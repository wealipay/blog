---
date: 2025-11-19 19:11
abbrlink: "globall"
categories: Nextjs教程
tags: nextjs
title:
hidden: true
---

{% folding Metadata: %}

- 作用：从 Next.js 中导入 Metadata 类型（用于定义页面的 SEO 元数据，如标题、描述等）。
- 注意：import type 表示仅导入类型（TypeScript 特性），不会增加运行时的体积
  {% endfolding %}

{% folding font.className: %}

- Next.js 中控制字体加载的核心配置
- className={font.className},这里的 font 是你通过 next/font 创建的字体对象（如之前的 Ma_Shan_Zheng),
- font.className 是 Next.js 自动生成的CSS类名，包含：
  - 字体家族定义（`font-family: xxx`）
  - 字体显示策略（`font-display: swap/optional`）
  - 可能包含的CSS变量（如果你配置了`variable`参数）
- 如果配置了variable，还会生成：
```ts
:root {
  --font-mashan: 'Ma Shan Zheng';
}

```

### 为什么必须这样用？

1. **性能优化**：
   - Next.js 会通过这个className自动：
     - 预加载字体文件
     - 托管字体到你的域名（避免第三方请求）
     - 实施`font-display`策略
2. **SSR支持**：
   - 服务端渲染时就能注入正确的字体CSS
   - 避免客户端闪烁（FOUC）
     {% endfolding %}

{% folding swap交换: %}

- 意思是：先显示系统默认字体，等自定义字体加载完成后再“交换”成自定义字体。
- 特点：页面不会出现空白，用户能立刻看到内容，但字体可能会有“跳动”。
  {% endfolding %}

{% folding optional可选的: %}

- 意思是：如果自定义字体在很短时间内（比如100ms）没加载完成，就不显示了，直接用系统字体。
- 特点：页面不会跳动，但有可能一直用默认字体，自定义字体只在加载极快时才生效。
  {% endfolding %}

{% folding layout.tsx: %}

```ts
import React from "react";
import type { Metadata } from "next";
import { Ma_Shan_Zheng } from "next/font/google";
import "./globals.css";

const font = Ma_Shan_Zheng({
  weight: "400",
  subsets: ["latin"],
  display: "optional",
  variable: "--font-mashan",
  preload: true
});

export const metadata: Metadata = {
  title: {
    default: "wealipay.top",
    template: "%s | wealipay.top"
  },
  description: "薅支付宝羊毛就访问wealipay.top",
  keywords: ["薅羊毛", "支付宝", "wealipay.top"],
  icons: {
    icon: "/favicon.ico"
  }
};

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-CN" className={font.className}>
      <body className="h-dvh h-full">{children}</body>
    </html>
  );
}

```

{% endfolding %}
